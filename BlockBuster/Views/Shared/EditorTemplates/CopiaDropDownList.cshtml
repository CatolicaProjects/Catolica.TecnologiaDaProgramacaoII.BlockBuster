
@{
    BlockBuster.Models.Repository db = new BlockBuster.Models.Repository();
    var list = db.Copias.OrderBy(c => c.Titulo.Nome).ToList();
    var selectList = new SelectList(list, "Id", "Nome", Model);     
}

@Html.DropDownListFor(model => model, selectList, "[Selecione]");